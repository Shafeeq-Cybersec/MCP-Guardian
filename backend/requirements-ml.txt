# Optional ML detection tier.
# Guardian runs fully without these - heuristic detectors cover every category.
# Installing them upgrades the corresponding detectors automatically at startup.

sentence-transformers==3.3.1   # semantic prompt-injection similarity
presidio-analyzer==2.2.355     # PII named-entity recognition
presidio-anonymizer==2.2.355
detoxify==0.5.2                # toxicity classification
groq==0.13.1                   # fast LLM explanations (also works via HTTP without this)
