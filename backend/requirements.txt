# --- Core (always required) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.20
httpx==0.28.1
redis==5.2.1

# --- Optional ML tier (see requirements-ml.txt) ---
# The engine degrades gracefully when these are absent. Install separately:
#   pip install -r requirements-ml.txt
